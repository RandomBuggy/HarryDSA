#include <stdio.h>

int main() {

    // ( ? -> no -> if yes push into the stack
    // ) ? -> no -> if yes pop out of the stack
    //
    // condition for a balanced expression
    // 1. while popping stack should not underflow
    // 2. at EOE, stack must be empty
    return 0;
}

